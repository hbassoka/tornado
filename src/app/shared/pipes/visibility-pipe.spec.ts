import { VisibilityPipe } from './visibility-pipe';

describe('VisibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new VisibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
